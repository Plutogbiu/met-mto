server:
  port: 8080

spring:
  application:
    name: mto-server
  datasource:
    url: jdbc:mysql://localhost:3306/mto?useUnicode=true&characterEncoding=utf8mb4&connectionCollation=utf8mb4_0900_ai_ci&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

mto:
  storage:
    local-path: D:/workspace/met-mto/uploads
  map:
    tencent:
      key: ""
